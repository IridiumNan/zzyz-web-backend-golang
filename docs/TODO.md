# TODO LIST

- Validate the check password on database

- Update database table posts 
  
  - markdown_content -> content (not assign type)
  
  - add a int format type (and mapping on the golang backend)
